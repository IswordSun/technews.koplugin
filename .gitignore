# macOS metadata
.DS_Store

# opencode runtime state
.omo/

# workspace field doc (kept on disk for local sessions, not committed)
AGENTS.md

# milestone snapshots (ZIP + bundle, self-contained)
snapshots/

# temp/partial artifacts
*.part
