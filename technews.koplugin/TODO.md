# 科技资讯订阅 — 待办清单（原型 → 成品）

当前状态：核心链路可用（RSS → 内容块+图片 → EPUB → 原生阅读器），
单源/合并、缓存、图片开关、菜单与手势入口都已就位，
主菜单单入口 → 全屏首页（Menu 全屏页；打开今日资讯/重抓/收藏/分源/设置），已内置 26 个源（2026-09-23 扩充）。
以下是离"成品"还差的细节，按优先级排列，供逐步打磨。

## A. 先做（小改动，体验提升明显）✅ 已完成

- [x] **EPUB 封面**：取本期第一张可用图片作封面，并生成独立封面页
      （OPF 声明 `cover-image` + `<meta name="cover">`，KOReader 书架可正常显示）。
- [x] **缓存自动清理**：启动时保留最近 7 期，自动删除旧 EPUB 与对应 .sdr 阅读状态。
- [x] **图片开关即时生效**：切换“包含图片”时自动清除当天缓存，下次打开重新生成。
- [x] **错误提示友好化**：区分“无网络 / 服务器不稳定 / 页面解析失败”，
      失败弹窗附“重试”按钮（ConfirmBox）。
- [x] ~~**缓存过期自动更新**~~（2026-09-21 按需求移除开关；缓存按天自然失效：换日自动重建，同日可用「重新抓取今日」）。

## B. 体验打磨（中等）

- [x] **“今日”时间窗口**：本地 0 点起过滤（2026-09-20 起改为严格当日，不回补）（window.lua，含单测）；
      顺手修正了时区 bug（RSS 真 GMT → 本地时间显示，之前差 8 小时）。
- [x] **抓取进度细分**：显示“来源 1/2 · 全文 3/12 · 图片 5/43”。
- [x] **两源去重**：同一条新闻两源都报时只保留一条（标题相似度/关键词匹配）。
- [x] **图片瘦身**：原图直嵌导致单期 ~12MB（已实施 IT之家 BCE 缩放宽800（超高图降级480）；CNBeta CDN 不支持缩放参数）。
      方案：调研 CDN 缩放参数（如 IT之家/百度云加速的 x-bce-process），
      或按 Content-Length 阈值跳过过大图片。
- [x] **目录层级化**：42 章单层目录太长，现已按来源分组（nav/ncx 按来源两级分组，单源保持平铺）。
- [x] **摘要模式补全**（取消）：2026-09-20 调研结论——IT之家 RSS 描述即完整正文（9/9 样本逐句比对，全文抓取净增 0 字），无需“全文抓取”开关。

## C. 成品化（大件）

- [x] **设置集中**：全屏主页集中全部入口（2026-09-21）；2026-09-22 重构为「设置」子菜单
      （管理收藏/订阅源设置/包含图片/清理全部缓存），「重新抓取今日」置首页第 2 位；
      条数上限与缓存保留期仍为内置默认（未做可调，需要时再加）。
- [ ] **真机验证**：Kindle 上完整跑一遍（慢网络 + 小内存最容易暴露问题）。
- [ ] **失败降级策略**：连接层失败与 5xx 自动重试（4xx 立即失败，2026-09-22 审计）；
      可再评估：并发抓取、指数退避、单条失败静默跳过。
- [x] **版本化打包**：`scripts/make_snapshot.sh` 一条命令产出可安装 ZIP + 完整历史 bundle，
      存 `snapshots/`（2026-09-21；首个快照 01 对应 tag v0.1.1）。
- [ ] **i18n**：界面文案目前硬编码中文（个人使用可选做）。

## D. 订阅源体系（2026-09-20 起）

- [x] **订阅源选择框架**：首次打开多选弹窗、订阅源设置子菜单、菜单与合并按启用源动态化
- [x] **补充订阅源**：爱范儿、极客公园、Solidot、少数派已接入（2026-09-20；默认停用，在「订阅源设置」勾选；每源含适配器 + 线上实测 + 单测）

## E. 阅读功能（2026-09-21 起）

- [x] **文章收藏**：阅读器内一键收藏/取消收藏；收藏为单篇快照 EPUB（不受缓存清理影响）；
      首页「我的收藏」列表与管理删除（2026-09-21）
- [x] **少数派排版优化**：小标题 / 清单 / 图注结构化渲染；剥离派早报内嵌评论区，评论头像不再混入（2026-09-21）
- [x] **首页全屏化与菜单重构**：TouchMenu（系统菜单式半屏面板）→ Menu 全屏页（标题栏/✕/页码栏）；
      菜单重构与关闭键盘快捷键字母列（2026-09-22）
- [x] **阅读交互打磨**：右上角「⋯」快捷菜单（目录/收藏/删除并返回/关闭并返回，可绑手势）、
      阅读器菜单「关闭并返回」、最后一页返回提示、目录去快捷键字母（2026-09-22）；
      实测回归修复：⋯ 首帧即显示、快捷菜单标题居中、「关闭/删除」后不再把书架带进数据目录
- [x] **代码审计与健壮化**（2026-09-22）：EPUB 流式写盘（峰值内存 66MB→36MB 实测）、写盘失败检测、
      收藏索引原子写、数据目录守卫、快捷菜单/删除的文档门禁、侧车精简与死代码清理；
      CRC32 实测 8MB/23ms（≈350MB/s，JIT），保持纯 Lua
- [x] **早报二级目录**（2026-09-22）：文内小标题（kind=heading）≥2 条时自动生成二级目录
      （正文加锚点、nav/NCX 嵌套一级），爱范儿早报可在目录里逐条跳转

## 已知取舍记录

- **“今日”的定义**：严格本地 0 点起（只取当日条目，不回补；2026-09-20 用户要求）。
  2026-09-21 清晨实测：各源夜间停更，早 6 点整期仅 1~2 条；用户确认维持严格、不清晨读即可。
- **时区**：各源 RSS 的 pubDate 为标准 RFC822（含 GMT/+0000/+0800，已实测），统一转本地时间显示与过滤。
- 图片策略：每条文章的全部图片（IT之家经 BCE CDN 缩放宽 800，超高图降级 480）、单期安全上限 150 张。
- 第二个源为雷锋网（RSS 描述即完整正文，20 条/次、约 10 条/日；
  CNBeta 因 .tw 域名境外 302→MSN、大陆直连不可达已停用，适配器保留未启用）。
- IT之家 RSS 描述即完整正文（2026-09-20 对 9 篇样本逐句比对，净增 0 字），故不提供全文抓取开关。
- 合并视图：按各已启用源的「合并条数」抓取后按时间倒序混排；默认 IT之家 30 + 雷锋网 10。
- 新增源特性（2026-09-20 接入，默认停用）：爱范儿（正文在 content:encoded，~6 条/日）、
  极客公园（描述即全文，~4 条/日，含微信图床图不可缩且不能带 Referer）、
  Solidot（纯文字无图，~8 条/日）、少数派（需逐篇抓正文，量少 ~2 条/日，图片必须带 Referer）。
- 自测钩子：`TECHNEWS_SELFTEST=1` 环境变量，启动 3 秒后自动打开合并版；
  必须用全局变量保护（插件 dofile 加载会重置模块级变量）。
